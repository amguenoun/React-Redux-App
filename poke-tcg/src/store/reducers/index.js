const initialState = {
    isFetching: false,
    pokemonCards: [],
    error: '',
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
