const initialState = {
    isFetching: false,
    pokemonCards: [0],
    error: '',
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
